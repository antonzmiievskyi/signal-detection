CLOUDFLARE_RADAR_TOKEN=
CRUX_API_KEY=
OPENAI_API_KEY=
APIFY_TOKEN=
SLACK_BOT_TOKEN=
SLACK_CHANNEL=

# Continuous-mode runner (docker-compose). All optional — defaults shown.
# PIPELINE_INTERVAL_SECONDS=1800   # seconds between pipeline iteration starts
# COMPANIES_FILE=companies.json    # which company list to monitor
# MIN_DURATION_MINUTES=5           # T_min before a candidate becomes an outage
